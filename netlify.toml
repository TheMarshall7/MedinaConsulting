[build]
  command = "npm run build"
  publish = "web/out"

[build.environment]
  NODE_VERSION = "22"
  # This is a static export, not Netlify's Next.js SSR runtime.
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
